# Dotfiles
My dotfiles 

# Usage
This repo assumes you have the following installed:
- zsh
- jenv
- fzf
- sdkman

1. Clone this repo into `~/.dotfiles`.
2. `cd ~/.dotfiles`
3. `./install`
4. Choose `i` (install)
